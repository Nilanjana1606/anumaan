% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostic_recommendations.R
\name{.probit_diagnostic_thresholds}
\alias{.probit_diagnostic_thresholds}
\title{Central thresholds for diagnostic-report interpretation}
\usage{
.probit_diagnostic_thresholds(
  ebfmi_min = 0.3,
  rhat_max = 1.01,
  rhat_severe = 1.05,
  treedepth_max = 0L,
  divergent_max = 0L,
  omega_small_eigen_threshold = 0.05,
  omega_small_eigen_fraction_warning = 0.5,
  omega_condition_number_warning = 50
)
}
\arguments{
\item{ebfmi_min}{Warn below this minimum chain E-BFMI. Default \code{0.3}
(Stan's own guidance threshold).}

\item{rhat_max}{Warn above this Rhat. Default \code{1.01}.}

\item{rhat_severe}{Structural Rhat above this is treated as severe rather
than borderline. Default \code{1.05}.}

\item{treedepth_max}{Treedepth-saturation count above this triggers the
treedepth failure mode. Default \code{0L} (any saturation).}

\item{divergent_max}{Divergent-transition count above this triggers the
divergence failure mode. Default \code{0L} (any divergence).}

\item{omega_small_eigen_threshold}{Per-draw smallest-eigenvalue-of-Omega
value below which a draw is counted as near-singular. Default \code{0.05}.}

\item{omega_small_eigen_fraction_warning}{Fraction of posterior draws
flagged near-singular (via \code{omega_small_eigen_threshold}) above
which Omega geometry is reported as a primary concern. Default \code{0.50}.}

\item{omega_condition_number_warning}{Median Omega condition number above
which it is called out explicitly. Default \code{50}.}
}
\value{
Named list of thresholds.
}
\description{
Single place these thresholds are defined, so the interpretation logic and
the report table stay in sync instead of drifting independently.
}
\keyword{internal}
