% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_worst_offender_diagnostics.R
\name{plot_probit_worst_offender_diagnostics}
\alias{plot_probit_worst_offender_diagnostics}
\title{Trace and rank plots for the worst-converging structural parameters}
\usage{
plot_probit_worst_offender_diagnostics(
  fit,
  draws,
  title_base = "",
  n_worst = 6L
)
}
\arguments{
\item{fit}{List returned by \code{fit_bayesian_multivariate_probit()}.}

\item{draws}{A \code{posterior::draws_array} (typically \code{fit$draws}).}

\item{title_base}{Character. Prefixed to plot titles.}

\item{n_worst}{Integer. How many parameters to select by each criterion
(Rhat, bulk ESS) before deduplicating. Default \code{6}.}
}
\value{
Named list with \code{trace} and \code{rank} ggplot objects, or
  \code{NULL} if no structural parameter diagnostics are available.
}
\description{
Auto-selects the union of the top \code{n_worst} parameters by Rhat and
the top \code{n_worst} by lowest bulk ESS (from
\code{fit$diagnostics_detail$monitored_parameters}), and shows their
chain trace and rank-overlay plots with human-readable facet labels
(covariate x class, or correlation pair) instead of raw Stan parameter
names -- enough to tell slow-mixing, chain separation, a sticky region,
and multimodality apart, which a single Rhat number cannot.
}
