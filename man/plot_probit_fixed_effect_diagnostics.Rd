% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fixed_effect_diagnostics.R
\name{plot_probit_fixed_effect_diagnostics}
\alias{plot_probit_fixed_effect_diagnostics}
\title{Fixed-effect coefficient diagnostics, split by contrast type and faceted
by antimicrobial class}
\usage{
plot_probit_fixed_effect_diagnostics(fit, title_base = "", ci_level = 0.95)
}
\arguments{
\item{fit}{List returned by \code{fit_bayesian_multivariate_probit()}.}

\item{title_base}{Character. Prefixed to plot titles.}

\item{ci_level}{Numeric. Credible interval level. Default \code{0.95}.}
}
\value{
Named list of ggplot objects (\code{other}, \code{hospital}),
  either of which may be absent if that group has no covariates in this
  fit. \code{NULL} if \code{fit$draws} or \code{fit$X_event} is missing.
}
\description{
Returns a named list of up to two ggplot objects: \code{other} (every
non-hospital-dummy covariate -- intercept, age, gender, etc.) and
\code{hospital} (hospital contrasts only, if the fit used hospital as a
fixed effect rather than a random effect). Both facet by antimicrobial
class rather than putting every class's coefficients on one axis.
}
