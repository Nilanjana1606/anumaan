% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{amr_config}
\alias{amr_config}
\title{Create AMR Preprocessing Configuration}
\usage{
amr_config(
  column_mappings = NULL,
  fuzzy_match = TRUE,
  strict_validation = FALSE,
  date_columns = NULL,
  hai_cutoff = 2,
  infer_department = TRUE,
  event_gap_days = 14,
  mortality_window = 14,
  age_bins = "GBD_standard",
  contaminant_method = "auto",
  mdr_definition = "CDC",
  intermediate_as_resistant = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{column_mappings}{Named list of column name mappings. Default uses
\code{default_column_mappings}.}

\item{fuzzy_match}{Logical. Enable fuzzy matching for column names.
Default TRUE.}

\item{strict_validation}{Logical. If TRUE, stops execution if required
fields are missing. If FALSE, issues warnings. Default FALSE.}

\item{date_columns}{Character vector of date column names to parse.
Default NULL auto-detects date-like columns by name pattern.}

\item{hai_cutoff}{Numeric. Number of days after admission to classify
as Hospital-Acquired Infection (HAI). Default 2.}

\item{infer_department}{Logical. Attempt to infer hospital department
from other variables if missing. Default TRUE.}

\item{event_gap_days}{Numeric. Minimum days between events to create
new event_id. Default 14.}

\item{mortality_window}{Numeric. Days after culture to classify death
as infection-related. Default 14.}

\item{age_bins}{Character. Age binning strategy: "GBD_standard",
"pediatric", or "geriatric". Can also be a custom vector. Default
"GBD_standard".}

\item{contaminant_method}{Character. Method for contaminant classification:
"auto" (cascade through available methods), "device_based",
"heuristic", "provided". Default "auto".}

\item{mdr_definition}{Character or numeric. MDR/XDR definition (used for
both): "CDC", "WHO", or numeric threshold for number of resistant
classes. Default "CDC".}

\item{intermediate_as_resistant}{Logical. Treat Intermediate (I) as
Resistant (R) except for special cases like Colistin. Default TRUE.}

\item{verbose}{Logical. Print progress messages during processing.
Default TRUE.}
}
\value{
An \code{amr_config} object (list with class)
}
\description{
Creates a configuration object that controls the behavior of the
preprocessing pipeline. Users can override default settings for their
specific use case.
}
\examples{
\dontrun{
# Use defaults
config <- amr_config()

# Customize for specific hospital
config <- amr_config(
  hai_cutoff = 3,
  mdr_definition = 5,
  strict_validation = FALSE
)
}
}
