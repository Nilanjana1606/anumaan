% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostic_recommendations.R
\name{.probit_classify_fitting_issues}
\alias{.probit_classify_fitting_issues}
\title{Classify why a fit's sampler diagnostics look the way they do}
\usage{
.probit_classify_fitting_issues(
  diag,
  grouped_diag,
  degeneracy_stats,
  residual_structure,
  beta_summary = NULL,
  thresholds = .probit_diagnostic_thresholds()
)
}
\arguments{
\item{diag}{\code{fit$diagnostics} (canonical scalar diagnostic fields).}

\item{grouped_diag}{\code{fit$diagnostics_detail$grouped} -- canonical
per-parameter_group Rhat/ESS table (rows: beta, Omega, lp__, z_free, any
declared random-effect blocks). May be \code{NULL}.}

\item{degeneracy_stats}{Result of \code{.omega_degeneracy_stats()}, or
\code{NULL} for identity-residual fits.}

\item{residual_structure}{\code{"identity"} or \code{"correlated"}.}

\item{beta_summary}{Per-covariate-family beta Rhat/ESS breakdown from
\code{plot_probit_beta_family_diagnostics()$summary}, or \code{NULL}.}

\item{thresholds}{List from \code{.probit_diagnostic_thresholds()}.}
}
\value{
Named list: \code{primary}, \code{secondary}, \code{not_indicated}
  (character vectors, possibly empty) and \code{bucket} (single string
  identifying which top-level failure mode was selected as primary, for
  testing).
}
\description{
Produces a ranked primary/secondary/not-indicated recommendation set
instead of one generic line, so a reviewer is told the SPECIFIC failure
mode (divergence vs. energy vs. Omega geometry vs. fixed-effect sparsity vs.
treedepth) rather than a boilerplate "increase adapt_delta or simplify the
model" that is only actually appropriate for divergences.
}
\keyword{internal}
